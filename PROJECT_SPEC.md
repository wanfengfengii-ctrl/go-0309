# 山岭隧道钢纤维湿喷混凝土支护闭合裁定服务

## 项目目标

建设一个可编译运行、以后端 HTTP API 为主要公共接口的 Go 服务，管理山岭隧道单次开挖循环内钢纤维湿喷混凝土的全过程证据。系统以“开挖循环—受喷面单元—喷射层—喷带”为只追加覆盖格，使用嵌入式持久化数据库保存锁定快照、材料账本、设备租约、轨迹、养护检测证据、缺陷传播、复喷代次和终局凭据，并在重启后恢复未完成事务、有效租约及待重试设备调用。实现规模限定为20至30个生产Go文件、2000至3000行有效生产代码，目标约2500行，划分为至少4个有实际职责的包；公开测试不少于4个测试文件和12个确定性测试用例。仓库包含go.mod、服务入口、数据库迁移或初始化逻辑，以及支持linux/amd64和linux/arm64构建的Dockerfile。

## 端到端业务流程

1. 锁定开挖循环：提交隧道、里程、循环、开挖轮廓、围岩分区及邻接表、受喷面网格、欠挖禁喷区、渗水点、设计厚度、材料与钢纤维批次、盘次计划、喷射层序与方向、喷嘴姿态窗口、回弹收集区、养护日程、测厚点、芯样与板试件映射、性能阈值和任务代次；校验摘要新鲜度、网格完整性、几何边界及定点运算后生成不可变快照。
2. 施工准备与材料联动：依次确认基面清理和渗水处置；按操作号原子获取拌合机、湿喷机、速凝剂泵、机械臂作业区、回弹收集区及检测通道的限时互斥租约，同时扣减水泥基料、骨料、水、速凝剂和钢纤维并创建盘次；相同操作号同内容重放返回原结果，内容不同则整体拒绝。
3. 分格分层喷射：记录泵送窗口、设备回执和机械臂轨迹，只允许受喷面单元沿锁定方向追加连续喷带，并按层形成连续前缀；校验喷带宽度、搭接、喷嘴距离、入射角、材料盘次、租约期限和禁喷区边界，任何失败均不得写入有效覆盖或扣减附加材料。
4. 回弹、养护与检测：按整数克封存称量回弹料、清洗回收物、试样、有效上墙量和合理损耗，禁止回弹料重新进入拌制；覆盖闭合后按逻辑时间追加养护证据，再关联厚度扫描、测厚针、芯样、板试件、压力或拉拔结果，使用确定性定点运算计算回弹率、有效厚度、纤维含量、早期强度增长率、粘结强度和空洞率。
5. 缺陷复喷复验：厚度不足、脱空、钢纤维离析、回弹超限、喷射冷缝或中断、渗水冲蚀、早期强度不足触发一次性传播计算；依据受喷面邻接、共同泵送窗口、同材料盘次和围岩松动分区生成排序稳定的唯一凿除复喷集合，登记凿除量并创建新代次，旧层及旧检测证据永久保留。
6. 终局裁定：在材料和回弹去向守恒、覆盖完整、喷射及养护前缀闭合、全部性能达标、缺陷复喷完成后，接收两名不同且合格人员的独立复核；支护闭合、风险隔离或取消通过数据库单写终局屏障竞争，只能产生一个不可变终局，旧代次迟到回执不得改变当前结论。

## 核心组件与职责

1. 围岩分区与喷射材料规则目录：管理不可变设计摘要、围岩分区、邻接关系、网格几何、禁喷区、渗水点、配合比、材料批次、喷嘴姿态窗口、性能阈值及检测映射，并提供整数几何和定点边界校验。
2. 喷射任务及覆盖聚合：维护开挖循环、受喷面单元、喷射层、喷带和任务代次的只追加状态机，验证方向、连续层前缀、喷带连续性、搭接和有效覆盖，生成确定排序的聚合视图。
3. 组分守恒与资源租约管理器：以整数克复式账本管理原料、有效上墙量、试样、回弹料、清洗回收物、凿除物和合理损耗，原子处理盘次、库存扣减、关键组分守恒、幂等记录与六类限时互斥租约。
4. 喷射轨迹及养护证据记录器：追加泵送、速凝剂联动、喷嘴姿态、回弹封存、养护、扫描和试验仪器证据；封装带逻辑时间、确定重试次数和脚本化结果的设备调用状态机。
5. 缺陷复喷与终局仲裁器：确定性计算缺陷传播集合和新代次，隔离旧代次迟到事件，校验双人复核及全部闭合条件，并通过单写屏障裁定闭合、风险隔离或取消。
6. Go HTTP API：提供JSON命令与查询接口、稳定错误码、请求校验、事务边界、恢复启动流程和健康检查；错误明细统一按隧道、里程、循环、围岩分区、受喷面单元、喷射层、喷带、代次排序。

## 领域规则与不变量

1. 所有长度使用整数毫米，角度和比例使用明确定标的定点整数，质量及钢纤维、速凝剂关键组分使用整数克；乘法、加法、除法须检查负值、除零、舍入规则、区间边界和64位溢出，退化区域或越界几何一律拒绝。
2. 锁定快照必须完整且内部一致：受喷面单元恰好覆盖设计喷射区域、单元之间不得非法重叠、不得侵入欠挖禁喷区；设计或配合比摘要陈旧、材料批次错配、检测映射缺失均不得进入施工态。
3. 覆盖数据只追加不改写。每个单元只能沿锁定方向形成连续喷带和连续层前缀；跳层、重复喷带、喷带断裂、搭接不足、错误盘次、过期租约或姿态越窗不能成为有效证据。复喷只能建立更高代次，并永久保留旧层、凿除量和检测证据。
4. 每个盘次满足“投入原料=有效上墙量+试样+回弹料+清洗回收物+合理损耗”的总质量守恒，并分别核对钢纤维和速凝剂去向；回弹料只能封存或处置，禁止回用。材料扣减、盘次创建和所需租约获取必须在一个事务中成功或全部回滚。
5. 租约由资源标识、持有者、逻辑起止时间和栅栏令牌组成；同一资源在有效区间内只允许一个持有者，过期令牌不能追加证据。钢纤维库存并发扣减以条件更新防止超领，恢复时按照持久化逻辑时间重建租约状态。
6. 设备拒绝、断连、超时或格式错误仅创建待重试调用，保存逻辑时间、尝试序号和确定性退避截止点；未取得合法读数不得推进覆盖、养护或检测前缀，也不得解除风险屏障。乱序回执按代次、调用序号和栅栏令牌过滤。
7. 缺陷传播以初始失败点为种子，对满足受喷面相邻、共同泵送窗口、同材料盘次或同围岩松动分区条件的相关覆盖作确定性闭包；集合以规范键去重并排序，同一证据重复触发必须得到同一复喷集合，代次冲突则不产生部分记录。
8. 操作号在作用域内唯一并绑定规范化请求摘要；相同内容重试返回原结果，内容冲突返回稳定错误码。所有写命令使用数据库事务和唯一约束，失败不得遗留材料扣减、租约、覆盖、回弹去向、养护证据或闭合凭据；终局一经写入不可改变。

## 数据模型与持久化

1. DesignSnapshot：隧道、起止里程、循环、版本摘要、开挖轮廓、围岩分区与邻接、受喷面单元多边形、禁喷区、渗水点、设计厚度、层序、喷带方向、姿态窗口、日程、阈值和锁定逻辑时间。
2. MaterialBatch与MixPan：水泥基料、骨料、水、速凝剂、钢纤维批次，盘次计划、泵送窗口、整数克投入量、关键组分量、库存版本和配合比摘要。
3. CoverageEvent：任务代次、受喷面单元、喷射层、喷带序号、几何范围、搭接长度、盘次、轨迹引用、有效厚度、逻辑时间、栅栏令牌及只追加事件状态。
4. MassLedgerEntry与Disposition：按盘次记录原料扣减、有效上墙、试样、回弹、清洗回收、凿除物、合理损耗及钢纤维和速凝剂分量；每项具有来源、去向和不可变流水号。
5. ResourceLease与DeviceCall：资源、持有者、期限、栅栏令牌；设备类型、调用参数摘要、逻辑时间、尝试次数、待重试截止点、回执或稳定故障类型。
6. Evidence：基面及渗水确认、喷嘴轨迹、回弹封存、养护覆盖、厚度扫描、测厚针、芯样、板试件、压力与拉拔结果，以及到单元、层、喷带、盘次和代次的映射。
7. DefectCase与RepairGeneration：缺陷类型、种子证据、传播规则版本、规范化复喷集合、凿除量、新代次、复验映射和完成状态；旧代次记录不可更新。
8. Review与TerminalDecision：人员资格快照、独立复核结论、请求摘要，以及闭合、风险隔离或取消三选一终局；OperationRecord保存幂等键、内容摘要、结果和错误码。

## 公开接口

1. POST /v1/cycles：创建并锁定开挖循环快照；GET /v1/cycles/{id}及GET /v1/cycles/{id}/coverage返回按规范键排序的状态与覆盖视图。
2. POST /v1/cycles/{id}/surface-confirmations、/mix-pans、/spray-bands、/rebound-seals和/cure-evidence：依赖顺序追加施工命令，每个写请求必须携带operation_id、逻辑时间、任务代次和必要租约令牌。
3. POST /v1/leases/acquire、/renew、/release：操作六类互斥资源；批量获取采用全有或全无语义，并以稳定资源顺序消除竞争结果的不确定性。
4. POST /v1/device-calls与POST /v1/device-calls/{id}/receipts：记录电子秤、流量计、姿态传感器、测厚扫描仪、压力机及拉拔仪的调用、失败、重试和回执。
5. POST /v1/cycles/{id}/tests、/defects和/repairs：追加检测结果、生成唯一传播集合、登记凿除并启动或完成复喷复验代次。
6. POST /v1/cycles/{id}/reviews与POST /v1/cycles/{id}/terminal-decisions：提交双人复核并竞争唯一终局；GET /v1/cycles/{id}/audit导出按领域键排序的完整证据链。
7. 所有失败响应为统一JSON结构：error_code、message、retryable、ordered_reasons和operation_id；稳定错误码至少覆盖STALE_SNAPSHOT、INVALID_GRID、NO_SPRAY_INTRUSION、BATCH_MISMATCH、MASS_CONFLICT、LEASE_CONFLICT、LEASE_EXPIRED、LAYER_OUT_OF_ORDER、BAND_DISCONTINUITY、OVERLAP_INSUFFICIENT、FIXED_POINT_OVERFLOW、DEVICE_RETRY_PENDING、GENERATION_CONFLICT、IDEMPOTENCY_CONFLICT及TERMINAL_ALREADY_SET。

## 失败边界

1. 锁定事务边界：任何摘要、几何、映射或数值错误使整个快照创建失败，不留下可施工的半锁定循环。
2. 备料事务边界：库存条件扣减、盘次创建、组分账本和多资源租约批量获取共同提交；批次错配、并发超领或任一租约冲突时全部回滚。
3. 喷射证据边界：覆盖前缀、轨迹引用、有效上墙量与回弹待收义务共同提交；层序、搭接、禁喷区、设备或令牌错误不得产生任何有效覆盖。
4. 设备边界：外部仪器结果被视为不可信输入，拒绝、超时和坏格式仅持久化可重试状态；重启后继续相同尝试序列，禁止用默认值补齐证据。
5. 复喷边界：传播闭包、凿除账目、新代次和复验映射一次提交；并发生成冲突返回同一已有结果或稳定冲突码，旧代次迟到事件只进入审计拒绝记录。
6. 终局边界：闭合、风险隔离和取消共享唯一数据库约束与比较交换条件；并发请求中仅一个成功，其余取得相同既有终局，提交失败不得遗留复核或凭据的部分状态。

## 验收标准

1. 锁定操作固定轮廓、围岩分区、完整且不非法重叠的受喷面网格、禁喷区、渗水点、材料与盘次、层序和方向、养护检测映射、阈值及代次；陈旧摘要、退化几何、负值、越界和定点乘加溢出均以稳定错误码拒绝。
2. 整数毫米与定点整数规则能确定校验层厚、喷带宽度、搭接、喷嘴距离、入射角、水胶比、速凝剂掺量、纤维含量、单位面积上墙量、回弹率、有效厚度、强度增长、粘结强度和空洞率，并明确处理舍入、除零、时间间隔和边界值。
3. 在同步并发测试中，材料扣减、盘次创建和租约获取保持原子性，钢纤维不能超领；所有整数克总质量及钢纤维、速凝剂组分守恒，回弹料不可回用，幂等同内容重试无重复副作用，冲突内容整体回滚。
4. 业务依赖严格按基面及渗水确认、拌制、泵送、分层喷射、回弹封存、养护、检测推进；每个单元仅形成连续层和喷带前缀，错误盘次、跳层、重复或断裂喷带、搭接不足、过期租约均不能写入有效证据。
5. 脚本化设备拒绝、断连、超时和格式错误只能生成具有逻辑时间及确定重试次数的待重试调用；重启恢复后结果一致，未成功回执不得推进前缀或解除风险屏障。
6. 七类缺陷能够依受喷面邻接、共同泵送窗口、材料盘次和围岩分区一次生成唯一、有序的凿除复喷集合；新代次重新覆盖相关单元、层和检测点，旧证据与凿除量保留，迟到回执不能改变当前代次。
7. 仅当质量和回弹去向守恒、目标面完整覆盖、喷射及养护前缀闭合、厚度与性能达标、复喷复验完成且两名不同合格人员独立复核后才生成唯一闭合凭据；闭合、风险隔离和取消并发只能产生一个终局。
8. 项目达到标准规模且保持单一工作流：20至30个生产Go文件、2000至3000行有效生产Go代码、至少4个业务包；提供go.mod、持久化与重启恢复、HTTP服务及amd64/arm64 Docker构建，并有至少4个公开测试文件、12个测试用例和10个可独立定位的缺陷点。

## 确定性测试场景

1. geometry_lock_test.go：完整固定网格锁定成功；网格缺口、非法重叠和禁喷区侵入分别返回稳定排序原因；退化多边形及面积乘加溢出被拒绝。
2. mass_lease_test.go：整数克总质量、钢纤维和速凝剂三重守恒；同步屏障下两个盘次并发领取钢纤维仅一个成功；多资源租约冲突使库存、盘次和全部租约回滚；回弹料回用被拒绝。
3. coverage_state_test.go：固定喷带映射按方向形成连续层前缀；跳层、重复喷带、断裂喷带及搭接边界不足不产生覆盖；租约恰在边界有效而过期一刻失效；错误材料盘次整体回滚。
4. fixedpoint_metrics_test.go：验证水胶比、掺量、纤维含量、单位面积上墙量、回弹率、厚度、强度增长率、粘结强度及空洞率的精确舍入；覆盖除零、负时间间隔、阈值等号和64位溢出。
5. device_recovery_test.go：脚本化电子秤超时、流量计坏格式和扫描仪断连产生确定尝试序列；数据库重开后恢复待重试调用；旧栅栏令牌或乱序回执不能推进喷射或检测状态。
6. repair_generation_test.go：由厚度不足种子沿邻接、共同窗口、同盘次和同围岩分区得到精确复喷闭包；重复触发返回同一集合；并发创建只保留一个新代次；旧代次迟到检测不改变复验结论。
7. idempotency_transaction_test.go：同操作号同JSON语义内容重复提交返回同一结果；同操作号不同内容返回IDEMPOTENCY_CONFLICT；故意在账本写入后触发约束错误，断言无部分材料、租约、覆盖、回弹或养护记录。
8. terminal_race_test.go：缺少任一守恒、覆盖、养护、检测、复喷或第二复核条件时拒绝闭合；两名相同人员不能通过；闭合、风险隔离和取消在同步屏障下竞争后只有一个终局且重启后不变。

## 组件追踪关系

1. 规则目录对应锁定流程、DesignSnapshot及geometry_lock_test.go，覆盖验收1、2中的网格、禁喷区、摘要和定点几何规则。
2. 覆盖聚合对应分格分层喷射、CoverageEvent及coverage_state_test.go，覆盖验收4中的方向、搭接、喷带连续性和层前缀。
3. 守恒与租约管理器对应备料和回弹流转、MaterialBatch、MixPan、MassLedgerEntry、ResourceLease及mass_lease_test.go，覆盖验收3和材料去向部分。
4. 轨迹及养护证据记录器对应设备、喷射、养护和检测流程，关联DeviceCall与Evidence，以及fixedpoint_metrics_test.go和device_recovery_test.go，覆盖验收5、6。
5. 缺陷复喷与终局仲裁器对应DefectCase、RepairGeneration、Review、TerminalDecision，以及repair_generation_test.go和terminal_race_test.go，覆盖验收7、8。
6. HTTP API负责全部命令事务、稳定错误响应、审计排序和幂等入口，关联OperationRecord与idempotency_transaction_test.go，并保证所有组件可通过公开API进行确定性测试。

## 独特性

本计划聚焦山岭隧道开挖循环中钢纤维湿喷混凝土的空间覆盖、整数组分守恒、回弹谱系、设备租约、缺陷传播复喷和初期支护终局裁定；其核心覆盖格与复喷代次证据链不同于既有标题中的锚杆拉拔、管片拼装、灌浆、植筋或其他材料验收项目。
